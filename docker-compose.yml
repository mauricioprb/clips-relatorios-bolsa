services:
  db:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_USER: ciclaro
      POSTGRES_PASSWORD: senha-segura
      POSTGRES_DB: ciclaro
    ports:
      - "5432:5432"
    volumes:
      - db-data:/var/lib/postgresql/data

  app:
    build: .
    depends_on:
      - db
    env_file:
      - .env
    environment:
      DATABASE_URL: ${DATABASE_URL:-postgresql://ciclaro:senha-segura@db:5432/ciclaro?schema=public}
    ports:
      - "3000:3000"
    restart: unless-stopped

volumes:
  db-data:
